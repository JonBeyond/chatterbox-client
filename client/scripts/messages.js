var Messages = {
  all : null,
  checkValidity: function() {
    //for (let entry in Messages.all.results) {
        //console.log(Messages.all.results[entry].username);  
    //all.results.entry.username = all.results.entry.username.replace(/\W/g, '');

//var obj = all.results[entry];
      
    //}
  
  }
};